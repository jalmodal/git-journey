# Pushing
Now that we have remote set up, let's push some work up to Github. To do this, we need to use the `git push` command.

We need to specify the remote we want to push up to AND the specific local branch we want to push to that remote.

`git push <remote> <branch>`